const BASE=new URL('./',self.location).pathname;const CACHE='epignosis-cache-v3';const ASSETS=[`${BASE}`,`${BASE}index.html`,`${BASE}style.css`,`${BASE}app.js`,`${BASE}assets/icon-192.png`,`${BASE}assets/icon-512.png`];self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(ASSETS))) });self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k))))) });self.addEventListener('fetch',e=>{const url=new URL(e.request.url);if(url.pathname.startsWith(BASE)){e.respondWith(caches.match(e.request).then(r=>r||fetch(e.request))) }});