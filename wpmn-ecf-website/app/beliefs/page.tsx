import {pageMetadata} from '@/lib/seo';
import {Shell,PageHero,TextLink,ConnectBand} from '@/components/site/ui';import {getPublicContent} from '@/lib/store';import {beliefs} from '@/lib/defaults';
export function generateMetadata(){return pageMetadata("Beliefs and Christ-Centered Teaching","Explore the finished work of Christ, union with Christ, life in the Spirit, resurrection hope, and Scripture read through Christ.","/beliefs");}export const dynamic='force-dynamic';
export default async function Beliefs(){const {settings:s}=await getPublicContent();return <Shell s={s}><PageHero label="BELIEFS & TEACHING" title={s['beliefs.heading']} intro={s['beliefs.intro']}><TextLink href="#start-with-christ">Start with Christ</TextLink></PageHero><section className="section wrap" id="start-with-christ">{beliefs.map(([title,body,refs,href],i)=><article className="belief-card" key={title}><h2>{s['belief.'+i+'.title']||title}</h2><div><p>{s['belief.'+i+'.body']||body}</p><p className="scripture-ref">{s['belief.'+i+'.scripture']||refs}</p><TextLink href={href}>Explore this teaching</TextLink></div></article>)}</section><section className="section campuses-section"><div className="wrap narrow"><p className="eyebrow">READING SCRIPTURE TOGETHER</p><h2>Bring your Bible. Bring your questions.</h2><p>We honor the whole Bible as Scripture. We pay attention to context, progressive revelation, and the fulfillment of God’s promises in Christ. We welcome honest questions and patient conversation as we learn together.</p><TextLink href="/contact?reason=general">Ask a question about our teaching</TextLink></div></section><ConnectBand s={s}/></Shell>}
