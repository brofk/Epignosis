import {pageMetadata} from '@/lib/seo';
import {Shell,PageHero,ContactDetails} from '@/components/site/ui';import {ContactForm} from '@/components/site/contact-form';import {getPublicContent} from '@/lib/store';
export function generateMetadata(){return pageMetadata("Request Prayer","Share a prayer request with Epignosis Christian Family and let us know if you would like pastoral follow-up.","/prayer");}export const dynamic='force-dynamic';
export default async function Prayer(){const {settings:s}=await getPublicContent();return <Shell s={s}><PageHero label="PRAYER & CARE" title={s['prayer.heading']} intro={s['prayer.intro']}/><section className="section wrap contact-layout"><aside><p className="eyebrow">RECEIVED WITH CARE</p><h2>You do not have to carry this alone.</h2><p>You may ask for prayer without asking for a reply. If you want a conversation, include a phone number or email and select follow-up.</p><p>You can also choose to share your request with pastors only.</p><ContactDetails s={s}/><p className="small-copy">This form is not monitored continuously. If someone is in immediate danger, contact local emergency services or someone nearby who can help.</p></aside><div className="form-panel"><ContactForm initialReason="prayer" fixed/></div></section></Shell>}
