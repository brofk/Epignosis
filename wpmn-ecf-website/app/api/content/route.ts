import {getPublicContent} from '@/lib/store';export const dynamic='force-dynamic';export async function GET(){try{const c=await getPublicContent();return Response.json({records:c.records.filter(r=>['sermon','article','series'].includes(r.kind))},{headers:{'Cache-Control':'no-store'}});}catch{return Response.json({error:'Teaching search is temporarily unavailable.'},{status:503});}}
