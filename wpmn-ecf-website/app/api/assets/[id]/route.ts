import {bucket,db} from '@/lib/store';export const dynamic='force-dynamic';
export async function GET(_req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(!/^[a-f0-9-]{36}$/.test(id))return new Response('Not found',{status:404});try{const asset=await db().prepare('SELECT type,name FROM assets WHERE id=?').bind(id).first<{type:string;name:string}>();if(!asset)return new Response('Not found',{status:404});const obj=await bucket().get(id);if(!obj)return new Response('Not found',{status:404});return new Response(obj.body,{headers:{'Content-Type':asset.type,'X-Content-Type-Options':'nosniff','Cache-Control':'public,max-age=86400',...(asset.type==='application/pdf'?{'Content-Disposition':'attachment; filename="teaching-notes.pdf"'}:{})}});}catch{return new Response('File temporarily unavailable',{status:503});}}
