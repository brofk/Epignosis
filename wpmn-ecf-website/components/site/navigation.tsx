'use client';
import Link from 'next/link';
import {usePathname} from 'next/navigation';
import {useState} from 'react';
import {Menu} from 'lucide-react';
import {Sheet,SheetTrigger,SheetContent,SheetHeader,SheetTitle,SheetDescription} from '@/components/ui/sheet';
const nav=[['Home','/'],['About','/about'],['Campuses','/campuses'],['Beliefs','/beliefs'],['Media','/media'],['Leadership College','/leadership-college'],['Give','/give'],['Contact','/contact']];
export function Navigation(){const path=usePathname(),[open,setOpen]=useState(false);return <header className="site-header"><div className="header-inner"><Link className="brand" href="/" aria-label="WPMN home"><span className="brand-monogram">W</span><span><strong>WPMN</strong><small>WORLD PRAYER & MISSIONS NETWORK</small></span></Link><nav className="desktop-nav" aria-label="Main navigation">{nav.map(([label,url])=><Link key={url} className={path===url||(url!=='/'&&path.startsWith(url))?'active':''} aria-current={path===url?'page':undefined} href={url}>{label}</Link>)}</nav><Link className="button header-visit" href="/visit">Plan a visit</Link><Sheet open={open} onOpenChange={setOpen}><SheetTrigger asChild><button className="menu-button" aria-label="Open menu"><Menu size={24}/></button></SheetTrigger><SheetContent className="mobile-sheet"><SheetHeader><SheetTitle>WPMN & ECF</SheetTitle><SheetDescription>Revealing Christ. Equipping His Family.</SheetDescription></SheetHeader><nav aria-label="Mobile navigation">{[...nav,['Plan a Visit','/visit']].map(([label,url])=><Link key={url} href={url} onClick={()=>setOpen(false)}>{label}</Link>)}</nav></SheetContent></Sheet></div></header>}
